caster_init_circle(16, 8);

my_name = "Upgrade Bench"

image_speed = 0
//image_index = round(random(image_number))
depth = -150

if store.current_task_index < 30 and store.horde_mode = 0 {instance_destroy()}
